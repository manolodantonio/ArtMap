<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- the theme applied to the application or activity -->
    <style name="MyActionBarTheme"
        parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="actionBarStyle">@style/MyActionBar</item>
    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBar"
        parent="Widget.AppCompat.Light.ActionBar">

        <item name="android:background">@color/teal_base</item>
        <item name="background">@color/teal_base</item>

        <item name="android:titleTextStyle">@style/ActionBarTitleStyle</item>
        <item name="titleTextStyle">@style/ActionBarTitleStyle</item>
    </style>

    <!--Action Bar Title Style-->

    <style
        name="ActionBarTitleStyle"
        parent="TextAppearance.AppCompat.Widget.ActionBar.Title">

        <item name="android:textStyle">bold</item>
        <item name="android:textSize">14sp</item>
        <item name="android:icon">@drawable/artmap_icon</item>
        <item name="android:showAsAction">always|withText</item>

    </style>


</resources>